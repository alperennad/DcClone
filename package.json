{
  "name": "private-livekit-voice-chat",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace apps/web\"",
    "build": "npm run build --workspace server && npm run build --workspace apps/web",
    "start": "concurrently \"npm run start --workspace server\" \"npm run start --workspace apps/web\"",
    "lint": "npm run lint --workspace apps/web",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
